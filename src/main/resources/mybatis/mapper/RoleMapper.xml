<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mdachu.mapper.RoleMapper">

    <insert id="save">
        insert into mdc_role (id, name, seq) values (
        #{id}, #{name},#{index}
        )
    </insert>




</mapper>